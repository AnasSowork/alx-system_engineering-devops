#!/usr/bin/env bash
# Define the filename for the private key, Generate the RSA key pair with the specified requirements
private_key="school"
ssh-keygen -t rsa -b 4096 -f "$private_key" -C "generated by Bash script" -N "betty"
